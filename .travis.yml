language: node_js
node_js:
  - 13

services:
  - docker

# Pre-testing installs
install:
  - echo "nothing needs to be installed"

# Scripts to be run such as tests
before_script:
  - echo "no tests"

script:
  - docker --version # print the version for logging
  - docker build -t udagram-api-feed ./udagram-api-feed
  - docker build -t udagram-api-user ./udagram-api-user
  - docker build -t udagram-frontend ./udagram-frontend
  - docker build -t udagram-reverseproxy ./udagram-reverseproxy

  - docker tag udagram-api-feed mpsijben/udagram-api-feed:latest
  - docker tag udagram-api-user mpsijben/udagram-api-user:latest
  - docker tag udagram-frontend mpsijben/udagram-frontend:latest
  - docker tag udagram-reverseproxy mpsijben/udagram-reverseproxy:latest

after_success:
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_USERNAME" --password-stdin
  - docker push mpsijben/udagram-api-feed
  - docker push mpsijben/udagram-api-user
  - docker push mpsijben/udagram-frontend
  - docker push mpsijben/udagram-reverseproxy